<div class="container">
  <div class="row">
    <div class="col-sm-12 text-center">

      <h2><%= @setlist.title %></h2>

        <!-- Placeholder for songlist -->
        <button onclick="addSong()", class="btn btn-primary">Click me</button>

        <div class="container">
          <div class="row">
            <div class="col-sm-4 text-center col-md-offset-4">

            </div>
          </div>
        </div>

      <!--<select>
        <% current_user.songs.each do |song| %>
        <option value="song.title"><%= song.title %></option>
        <% end %>
      </select>
      <br>-->

      <h4><%= link_to "Back", setlists_path %></h4>
    </div>
  </div>
</div>

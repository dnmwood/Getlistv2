<div class="container">
  <div class="row">
    <div class="col-sm-12 text-center">

      <h2>DASHBOARD</h2>

        <h4><%= link_to "Manage Your Songbook", songs_path %></h4>
        <h4><%= link_to "Manage Your Setlists", setlists_path %></h4>
        <% if current_user.setlists.empty? %>
        <h4><%= link_to "Create New Setlist", new_setlist_path %></h4>
        <% end %>
    </div>
  </div>
</div>
